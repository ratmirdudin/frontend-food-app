import React from 'react';
import {useTitle} from "../../hooks/useTitle";

const ContactsPage = () => {
    useTitle("Контакты")
    return (
        <div>
            CONTACTS PAGE WORKS!
        </div>
    );
};

export default ContactsPage;