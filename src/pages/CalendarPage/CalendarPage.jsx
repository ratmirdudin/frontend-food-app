import React from 'react';
import {useTitle} from "../../hooks/useTitle";

const CalendarPage = () => {
    useTitle("Календарь")
    return (
        <div>
            CALENDAR PAGE WORKS!
        </div>
    );
};

export default CalendarPage;